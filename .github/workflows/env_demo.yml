
name: Show environment variable

on: 
  workflow_dispatch:

env:
  NAME: Bhagyajeet

jobs:
  Show_variable:
    runs-on: ubuntu-latest
    steps:
      - name: Show plain text env
        run: echo "My name is ${{ env.NAME }}"

      - name: Show Secrets
        run: echo "One fun secret is ${{ secrets.BJ_SECRET }}"
    
